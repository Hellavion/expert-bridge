# Expert Bridge

Система управления экспертами, кураторами и клиентами с промокодами.

## О проекте

Expert Bridge - это веб-приложение для управления базой экспертов, кураторов, продуктов и клиентов. Система позволяет отслеживать регистрации клиентов по промокодам и управлять связями между сущностями.

### Основные возможности

- 📝 **Регистрация клиентов** через промокоды
- 👥 **Управление экспертами** с комиссиями и бонусами
- 🎯 **Управление кураторами** с привязкой к экспертам
- 📦 **Управление продуктами** с ценами в рублях
- 🎫 **Управление промокодами** с отслеживанием использования
- 💰 **Отслеживание оплат** клиентов
- 🔐 **Двухфакторная аутентификация** для администраторов
- 🌓 **Темная/Светлая тема** с автоматическим определением
- 🔒 **Защита от индексации** поисковыми роботами

## Технологии

### Backend
- **Laravel 12** - PHP фреймворк
- **Laravel Fortify** - аутентификация с 2FA
- **Inertia.js** - связка между Laravel и Vue
- **MySQL** - база данных (production)
- **SQLite** - база данных (development)

### Frontend
- **Vue 3** - JavaScript фреймворк
- **TypeScript** - типизация
- **Tailwind CSS v4** - стилизация
- **Reka UI** - компоненты
- **Vite** - сборщик

## Структура проекта

```
Bridge/
├── app/
│   ├── Http/Controllers/
│   │   ├── Admin/                 # Админ контроллеры
│   │   └── ClientRegistrationController.php
│   ├── Models/                    # Eloquent модели
│   └── Http/Middleware/
│       └── PreventIndexing.php    # Защита от индексации
├── database/
│   └── migrations/                # Миграции БД
├── resources/
│   ├── js/
│   │   ├── pages/                 # Vue страницы
│   │   ├── components/            # Vue компоненты
│   │   └── layouts/               # Layouts
│   └── views/
│       └── app.blade.php          # Главный template
├── routes/
│   ├── web.php                    # Основные маршруты
│   └── settings.php               # Маршруты настроек
├── public/
│   ├── build/                     # Собранный фронтенд
│   └── robots.txt                 # Запрет индексации
├── DEPLOYMENT.md                  # Инструкция по деплою
└── .env.production.example        # Production конфиг
```

## Установка и запуск

### Требования
- PHP 8.2+
- Composer
- Node.js 18+
- MySQL 8.0+ (production) или SQLite (development)

### Локальная разработка

```bash
# Клонируем develop ветку
git clone -b develop https://github.com/Hellavion/expert-bridge.git
cd expert-bridge

# Устанавливаем зависимости
composer install
npm install

# Настраиваем окружение
cp .env.example .env
php artisan key:generate

# Создаем базу данных (SQLite)
touch database/database.sqlite

# Запускаем миграции
php artisan migrate

# Запускаем dev окружение
composer dev
# Или отдельно:
# php artisan serve
# npm run dev
```

Приложение будет доступно по адресу: http://localhost:8000

### Production деплой

Полная инструкция по деплою на хостинг: [DEPLOYMENT.md](DEPLOYMENT.md)

**Кратко:**
1. Клонируйте `main` ветку на хостинг
2. Настройте `.env` с MySQL credentials
3. Установите зависимости: `composer install --no-dev`
4. Запустите миграции: `php artisan migrate --force`
5. Оптимизируйте: `php artisan optimize`

## Ветки Git

- **`main`** - production ветка (только собранный код)
- **`develop`** - разработка (весь исходный код)

## Важные команды

```bash
# Разработка
composer dev              # Запуск dev сервера + vite + queue
npm run dev              # Только vite dev server
composer test            # Запуск тестов

# Сборка
npm run build            # Production build

# Качество кода
npm run format           # Prettier форматирование
npm run lint            # ESLint проверка
php artisan pint        # PHP форматирование

# База данных
php artisan migrate      # Запустить миграции
php artisan migrate:fresh # Пересоздать БД
php artisan tinker       # Интерактивная консоль
```

## Лицензия

Частный проект.

## Контакты

- GitHub: https://github.com/Hellavion/expert-bridge
- Domain: https://expert-bridge.ru
